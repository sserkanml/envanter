import 'package:flutter/widgets.dart';

class BottomIconCustom extends StatelessWidget {
  const BottomIconCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [],
    );
  }
}

import 'package:flutter/material.dart';

class BusinessStepView extends StatelessWidget {
const BusinessStepView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold();
  }
}
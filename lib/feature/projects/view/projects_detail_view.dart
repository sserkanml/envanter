import 'package:flutter/material.dart';

class ProjectsDetailView extends StatelessWidget {
const ProjectsDetailView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold();
  }
}
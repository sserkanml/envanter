import 'package:flutter/material.dart';

class CustomSelectLocationView extends StatelessWidget {
const CustomSelectLocationView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold();
  }
}
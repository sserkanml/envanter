import 'package:flutter/material.dart';

class DetailLocationView extends StatelessWidget {
const DetailLocationView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold();
  }
}